@model IEnumerable<Wardrobe.Models.Top>

@{
    ViewBag.Title = "Index";
}

<h2 class="homepageCat">I've Got Tops</h2>

<p class="viewLabels">
    @Html.ActionLink("I Got Another!", "Create")
</p>

<div class="row container-fluid">
    @foreach (var item in Model)
    {

        <div class="row container-fluid col-sm-6 col-md-4">
            <div class="thumbnail">
                <img class="images" src="@Html.DisplayFor(modelItem => item.Photo)" alt="...">
                <div class="caption">
                    <h3 class="viewTitles"> @Html.DisplayFor(modelItem => item.Name) </h3>
                    <p class="viewLabelsDetails"> @Html.ActionLink("Details", "Details", new { id = item.TopID }) </p>
                    <p class="viewLabels">   @Html.ActionLink("Edit", "Edit", new { id = item.TopID })</p>
                    <p class="viewLabels"> @Html.ActionLink("Got Rid of It", "Delete", new { id = item.TopID })</p>
                </div>
            </div>
        </div>
    }

</div>
        @*<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Photo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TopType)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Color.ColorName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Occasion.OccasionType)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Season.TheSeason)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Photo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TopType)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Color.ColorName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Occasion.OccasionType)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Season.TheSeason)
                    </td>
                    <td>*@



        @*</td>
            </tr>*@
    }

</div>

